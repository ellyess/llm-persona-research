# Pinned to the versions used to produce the committed results.
# Python 3.11+ (Docker uses 3.11; developed on 3.12). These are the only
# third-party dependencies; everything else is the standard library.
anthropic==0.107.1
matplotlib==3.9.2
numpy==2.2.6
seaborn==0.13.2
pandas==2.2.2
